# Environment Variables Template
# Copy this file to .env and fill in your actual values
# The .env file is gitignored and will not be committed

# Moonshot AI API Key (also works as KIMI_API_KEY)
# Get your API key from: https://platform.moonshot.ai/
# Note: Moonshot AI provides the Kimi API, so MOONSHOT_API_KEY and KIMI_API_KEY are the same key
# This is used for LLM integration (TASK-005 and beyond)
MOONSHOT_API_KEY=sk-your-moonshot-api-key-here

# Alternative name (same key as MOONSHOT_API_KEY)
# Some project scripts use KIMI_API_KEY, but it's the same value
# KIMI_API_KEY=sk-your-moonshot-api-key-here

# Server Configuration (optional)
# PORT=3000
# NODE_ENV=development

# Supabase (for agent memory persistence and player state — TASK-012/013)
# Create a project at https://supabase.com, then go to Settings > API
# See docs/supabase-setup-guide.md for full setup instructions
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Note: Variables prefixed with VITE_ are exposed to the client.
# DO NOT use VITE_ prefix for API keys or secrets!
